-- plugin displays the current context (e.g., function or class name) while scrolling through code
return {
  "nvim-treesitter/nvim-treesitter-context",
  dependencies = {
    "nvim-treesitter/nvim-treesitter",
  },
}
